export enum EAuthAction {
    CREATE_SESSION = 'CREATE_SESSION',
    JOIN_SESSION = 'JOIN_SESSION',
}

export enum EProccessStatus {
    IDLE = 'IDLE',
    PENDING = 'PENDING',
    SUCCESS = 'SUCCESS',
    ERROR = 'ERROR',
}
